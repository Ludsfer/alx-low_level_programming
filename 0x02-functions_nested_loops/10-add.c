#include "main.h"
#include <stdio.h>
/**
 * add - function that adds two integers and returns the result
 * @a: first fun argument
 * @b: second fun argument
 * Return: returns the sum of the function
 */
int add(int a, int b)
{
return (a + b);
}
